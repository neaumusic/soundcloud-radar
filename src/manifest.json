{
  "manifest_version": 3,
  "version": "${PACKAGE_VERSION}",
  "name": "Soundcloud Radar",
  "description": "${PACKAGE_DESCRIPTION}",
  "content_scripts": [
    {
      "matches": ["*://*.soundcloud.com/*"],
      "js": ["main.js"]
    }
  ],
  "permissions": [
    "storage"
  ]
}
